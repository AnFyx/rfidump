# Outils de test sur PC, en mode simulation (non requis sur le Raspberry Pi).
# N'inclut pas requirements.txt : la bibliothèque du lecteur RC522 dépend de
# RPi.GPIO et spidev, qui ne s'installent que sur un Raspberry Pi.
# Sur le Pi, Flask vient des paquets apt (voir README).
flask==3.1.3
pytest==9.1.1
